import React, { Component } from 'react'
import { Button, AppRegistry, View } from 'react-native'

export default class Buttons extends Component {

    render() {
        <View style={{flex: 1, flexDirection: 'row'}}>
        
        </View>
    }
}